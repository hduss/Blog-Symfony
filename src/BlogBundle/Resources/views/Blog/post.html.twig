{% extends "BlogBundle::base.html.twig" %}


{% block content %}


		<div id="post">
			
				<h2>{{ post.title }}</h2>

				<p>{{ post.content }}</p>
				<p>{{ post.slug }}
		</div>


		<a href="{{ path('blog_index') }}">retour</a>


{% endblock content%}